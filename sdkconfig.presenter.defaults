# ============================================================================
# DEPRECATED -- retained as a placeholder only.
#
# All PC Controller (presenter) configuration has been merged into
# sdkconfig.defaults. This file is no longer loaded by ESP-IDF builds; the
# SDKCONFIG_DEFAULTS CMake / idf.py flag references sdkconfig.defaults only.
#
# Do not add new options here. Add them to sdkconfig.defaults so that the
# default demo profile and the PC Controller presenter profile stay in lock
# step with a single source of truth.
#
# Historical layering contract (now obsolete):
#   SDKCONFIG_DEFAULTS="sdkconfig.defaults;sdkconfig.presenter.defaults"
# ============================================================================
